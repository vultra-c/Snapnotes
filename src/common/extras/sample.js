/**
 * 闪念小抄 - 扩展知识点示例包
 *
 * 结构与 knowledgeData.js 相同：
 *   { "科目名": [ { id, title, desc, points, formulas? } ] }
 *
 * 此为空示例包，仅作 extrasRegistry 占位。
 * 后期新增扩展包：在 extras/ 下建 xxx.js，加一行 import + 在 knowledgeStore.js 注册即可。
 */
export default {}
